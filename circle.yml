dependencies:
  override:
    - npm install
    - npm install -g karma

test:
  override:
    - webpack
    - webpack --config webpack.config.min.js
    - ./node_modules/karma/bin/karma start --single-run